/*
    Name: assessment == QuestionaireResponse
    Algebra: assessment X message = Operad[PAC(CareService) \ Questionaire]
        partOf(Procedure)
        questionnaire(Questionnaire)
            uri
            id
            version
            context
            type

        status
        authored
        item.<> == Questionnaire/question.<>
*/

Grammar:	Map 5.0
Namespace:	assessment
Target:     FHIR_R4

Assessment maps to QuestionaireResponse:
    assessmentId maps to identifier
    timeStamp maps to authored
    Status maps to status

   question.answer maps to item.answer

    assessmentSchema maps to questionnaire
    assessmentOperand maps to partOf

AssessmentDocument maps to Questionaire
    resourceLocation maps to url
    documentId maps to identifier
    Version maps to version
    clinicalContext maps to useContext
    documentCode maps to code

    //question maps to item
    //Want to call question in Assessment isomorphic to question in Document (trivially same operational concept), fyi for future bundling